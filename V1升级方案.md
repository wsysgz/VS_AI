# V1升级方案

> 适用仓库：`D:\GitHub\auto`
>
> 最后更新：2026-04-27

这份方案不是操作手册，而是 VS_AI 的阶段性路线图。它回答的是：接下来几年别把工程做歪，当前 V1 到底先做什么、后做什么。

## 1. V1 的定义

对当前仓库来说，V1 不是“推倒重来重构一遍”，而是：

> **保留现有 `collect -> analyze -> render -> publish` 主链路，把来源稳定性、AI 可控性、运行可观测性补成一个可持续维护的系统。**

具体表现为：

- 来源入口不再主要依赖脆弱网页 listing
- AI 调用不再只是“能用”，而是可切换、可观测、可治理
- 运行态不再靠猜，而是统一回到 `run-status.json`
- 文档、交接、验收路径稳定，换人或失忆后能快速恢复

## 2. 当前执行状态（2026-04-27）

### 已完成

- [x] 飞书交付主路径已完成真实送达验证
- [x] 文档主入口收口到仓库根目录
- [x] `AI对接手册` 强化为 AI / 接手者总入口
- [x] 第一轮坏源清洗
- [x] `source_health` per-source breakdown
- [x] `source_health` failure-only 统计
- [x] 统一 `source_registry` builder
- [x] `source_governance` 队列与 JSON artifact
- [x] ops dashboard 的 `Source Failure Breakdown`
- [x] ops dashboard 的 `Source Registry` / 治理队列表格
- [x] `arxiv-cs-ai` 切到官方 Atom 查询接口
- [x] 本地验证基线更新到 `333 passed`
- [x] `deepseek-updates` 切到结构化页面抓取
- [x] `qwen-blog` 切到官方 JSON API
- [x] `moonshot-blog` 切到结构化页面抓取
- [x] `google-ai-edge` 切到稳定 RSS feed
- [x] `openvino-blog` 切到结构化页面抓取
- [x] `meta-ai-blog` 切到 Meta Newsroom 技术与创新 feed
- [x] `st-blog` 切到 ST 官方 AI tag feed
- [x] `ti-e2e-blog` 切到 TI 官方 newsroom JSON 接口
- [x] `nxp-edge-ai` 停止绑定失效页面，改为官方 GitHub 替代方向
- [x] `YouTube V1` 落地并保留已验证稳定白名单（Google Developers / NVIDIA）
- [x] 明确“本地优先验证、阶段完成才跑远端 workflow”的工程规则
- [x] LiteLLM Gateway 接入、stage alias smoke、本地回退路径完成收口
- [x] Langfuse tracing / prompt eval tracing / fallback / budget guardrail 已落地
- [x] `P3-A` 首轮发布级验收完成：飞书静态卡片真实发送成功，文本 fallback 保留
- [x] `diagnose-delivery` 已支持 `--channels feishu`，可做 Feishu-only 验证
- [x] `2026-04-22` 本地真实 Feishu 主路径已验证为 `card_success`
- [x] `2026-04-22` `run-status` / delivery diagnose 已显式区分 `card_success / text_fallback`
- [x] 飞书 sidecar 已打通：日报文档、治理表、治理任务列表可本地同步
- [x] `2026-04-24` 最新远端 `Collect And Report` 已成功（run `24892691284` / commit `a2f1455`）
- [x] `2026-04-24` `Delivery Canary` 已远端成功（run `24864175041`）
- [x] `2026-04-24` `Source Reachability Canary` 已远端成功（run `24864003455`）
- [x] DeepSeek 默认路由已切到 V4：`analysis / summary / forecast = pro`，`prefilter / discovery / search = flash`
- [x] GitHub Repository Variables 已切到 DeepSeek V4 路由
- [x] 英文推送根因已在本地修正：summary/forecast prompt 强制中文输出，英文结果会触发中文 fallback
- [x] `renesas-blog` / `youtube-google-developers` / `youtube-nvidia` 已做活跃采集降噪处理，等待稳定入口恢复后再启用
- [x] `P3-B` 第二轮 polish 已阶段性收口：现有飞书表已支持旧状态迁移、第一屏可写字段前置、视图过滤重放，旧版英文资源已删除或改名停用，轻自动化本阶段不新增
- [x] `P3-C` 国内外对比分析（A 方案）已完成：`P3-C-1` 标签层、`P3-C-2` comparison brief / 交付消费、`P3-C-3` 本地验收 / 文档 / 发布收口均已闭环
- [x] 公开站首轮优化已完成：搜索输出转义、安全链接、favicon、搜索标签去重均有测试保护
- [x] 交付面已收敛为飞书单通道；旧非飞书渠道已从主链、workflow、配置和文档中移除
- [x] 本地验证策略已改为 Codex 主导：本地验证由 Codex 执行并审查，DeepSeek 仅作为运行态输出/历史分析的对照对象
- [x] 本阶段本地完整验证链已跑完：workflow guard、全量 pytest、prompt eval、reviewed run-once、Pages / ops / governance / review queue 构建均已通过

### 已收口尾项

- [x] 第一批 RSSHub 候选/稳定 feed 首批落地（`arm-news` / `nvidia-embedded`；`anthropic-news` 已以远端验证通过的 validated listing 收口）
- [x] 第一版 changedetection.io watch 清单
- [x] 将治理队列进一步转成运维优先级视图
- [x] AI Gateway / tracing 统一接入
- [x] 双 AI 并存并按阶段分工（`analysis / summary / forecast`）
- [x] GitHub Actions 改为支持可切换 provider，而不是当前固定走 DeepSeek
- [x] P2-A 远端 stage-level routing 已完成验证（`analysis/forecast=DeepSeek`，`summary/prefilter=MiniMax`）
- [x] `run-status.json` 已显式区分 `card_success / text_fallback`
- [x] `P3-C` 国内外对比分析（A 方案）已完成，下一步已进入公开站与交付面收口

### 当前待推进

- [ ] 阶段完成后按需做一次远端发布级确认，并记录 run id
- [ ] 持续观察 `renesas-blog` blocked 状态，不在本阶段扩源
- [ ] 下一阶段再评估公开站第二轮内容组织 / Pages 信息架构

### P1 收口判断（2026-04-18）

- [x] P1 的核心工程目标已经完成：高价值脆弱来源已迁移到更稳入口，主链运行可继续推进
- [x] P1 不再阻塞主线开发，可转入“运营尾项 + 持续治理”模式
- [x] P1 运营尾项已不再存在明确阻塞项；残余工作转入持续治理模式

## 3. 为什么当前优先级已切到 P3

当前系统最痛的已经不再是“来源治理基础能力缺失”，而是：

1. 飞书已经成为当前最重要的交付与协作面，需要把“能发”升级成“好用、可验、可运维”
2. `P3-A` 已完成首轮真实验证，并已把 `Delivery Canary` 收口到飞书卡片主路径；当前剩余以交付面小修为主
3. `P3-B` 已形成第一版飞书多维表格运营台，并完成第二轮 polish 的阶段性收口；后续只保留运营维护与小修

`P3-C` 已完成后，下一步才进入公开站整理 / 优化方案讨论；未确认具体方案前，不回头混做 P1 / P3-B 尾项，也不直接开始大规模服务化重构。

## 4. 当前治理快照

根据仓库内现有治理 artifact（2026-04-22）：

- `manual_review_count = 1`
- `rsshub_candidate_count = 4`
- `changedetection_candidate_count = 4`
- `replacement_candidate_count = 5`

说明 P1 已经进入小尾项阶段：真正仍需持续盯住的来源问题，已经收敛到少量高优先级项，而不是大面积失控。

当前优先级最高的来源示例：

- `nxp-edge-ai`
- `cerebras-blog`
- `edge-impulse-blog`
- `espressif-blog`
- `renesas-blog`
- `zhipu-news`

## 5. 分阶段路线图

### P0：基础收口（已完成）

目标：先把“系统能跑、能看、能验证、能交接”稳住。

已完成：

- 飞书交付主路径验证
- 文档收口
- 验证基线固定
- 来源治理基础设施铺底

### P1：来源稳定性升级（阶段性收口）

目标：把“反复坏掉的网页来源”从被动修修补补，升级成可治理的来源体系。

工作包：

1. 建立来源分层
   - `stable-feed`
   - `fragile-listing`
   - `dynamic-site`
   - `manual-watch`
2. 给高价值脆弱来源优先寻找 RSSHub route 或稳定 feed
3. 给无稳定 feed 的来源建立 changedetection 旁路
4. 为 manual-review 来源补明确替换方案
5. 把治理候选转成真正可执行的运维清单

P1 的退出条件建议：

- 至少一批高价值 fragile source 已切到稳定 feed / RSSHub
- changedetection watch 清单已建立并有可追踪记录
- manual-review 来源都已有明确去向
- `source_governance` 能直接指导值班决策，不只是列数据

当前判断（2026-04-18）：

- [x] 高价值 fragile source 已完成第一轮稳定入口迁移
- [x] changedetection watch 清单已正式落表
- [x] manual-review 来源已经全部有明确去向或替代方向
- [x] `source_governance` 已强化为更直接的值班优先级视图
- [x] `anthropic-news` 已完成收口：官方 RSS/feed 候选当前 404，保留远端已验证通过的官方 news listing 作为正式入口

### P2：AI 层升级

目标：把当前 AI 调用从“能用”升级成“可控、可切换、可回溯”。

当前已知起点：

- `src/auto_report/integrations/llm_client.py` 已支持 OpenAI-compatible provider
- 当前已确认可直接切换的 provider：
  - DeepSeek 官方：`AI_PROVIDER=deepseek`
  - MiniMax-M2.7（通过 OpenAI-compatible 第三方入口）：`AI_PROVIDER=minimax_svips`
- 当前已完成能力：
  - `analysis / summary / forecast` 已支持按阶段切换不同 provider
  - `pre_filter` 已支持独立于 `analysis` 的 provider 设置
  - 已预留 helper stage 合同：`discovery` / `search`
- 当前 GitHub reusable workflows 已支持读取统一的
  `AI_PROVIDER / AI_BASE_URL / AI_MODEL / AI_API_KEY` 配置
  - 默认远端行为仍保持 DeepSeek
  - 当 Repository Variables / Secrets 配置完成后，线上远端也可读取
    `ANALYSIS_* / SUMMARY_* / FORECAST_*` 的 stage-level provider 配置
- 当前已完成远端验证：
    - `analysis` -> DeepSeek
    - `summary` -> MiniMax-M2.7
    - `forecast` -> DeepSeek
    - `prefilter` -> MiniMax-M2.7
  - 当前下一轮默认建议已更新为：
    - `analysis` -> `deepseek-v4-pro`
    - `summary` -> `deepseek-v4-pro`
    - `forecast` -> `deepseek-v4-pro`
    - `prefilter` / `discovery` / `search` -> `deepseek-v4-flash`

推荐工作包：

1. 接入 LiteLLM Gateway
2. `llm_client` 支持直连 / Gateway 切换
3. 接入 Langfuse traces
4. 把 prompt eval 与线上 run 打通统一 trace 维度
5. 增加 provider fallback、budget guardrail、per-stage latency / token budget
6. [x] 支持 stage-level model routing（例如 `analysis=DeepSeek`, `summary=MiniMax`, `forecast=DeepSeek`）
7. [x] 让 GitHub Actions 读取统一的 `AI_PROVIDER / AI_BASE_URL / AI_MODEL / AI_API_KEY` 配置

P2 中关于双 AI 并存的建议默认分工：

- `analysis`
  - 优先使用更稳、成本更可控的 provider（当前建议 DeepSeek）
- `summary`
  - 优先使用更擅长综合整理和最终表达的 provider（可评估 MiniMax-M2.7）
- `forecast`
  - 初期可继续沿用 DeepSeek，后续再根据真实效果决定是否切给 MiniMax
- `pre_filter`
  - 当前建议直接交给 MiniMax-M2.7，用于候选压缩和降噪
- `discovery` / `search`
  - 当前建议交给 MiniMax-M2.7，用于探源、搜索结果整理、候选映射输出

这样做的目标不是“同时开两个模型就算升级”，而是：

- 让高频阶段优先考虑成本与稳定性
- 让低频但更看重表达质量的阶段使用更合适的模型
- 在不重写主链路的前提下，把双 AI 并存变成可配置、可观测、可回滚的能力

GitHub / Repository secrets & variables 规划建议：

- Repository Secrets
  - `DEEPSEEK_API_KEY`
  - `AI_API_KEY`（给 MiniMax 或未来第三方 OpenAI-compatible provider）
- Repository Variables
  - `AI_PROVIDER`
  - `AI_BASE_URL`
  - `AI_MODEL`
- Stage-level Repository Variables
  - `ANALYSIS_AI_PROVIDER`
  - `ANALYSIS_AI_BASE_URL`
  - `ANALYSIS_AI_MODEL`
  - `SUMMARY_AI_PROVIDER`
  - `SUMMARY_AI_BASE_URL`
  - `SUMMARY_AI_MODEL`
  - `FORECAST_AI_PROVIDER`
  - `FORECAST_AI_BASE_URL`
  - `FORECAST_AI_MODEL`
- Stage-level Repository Secrets
  - `ANALYSIS_AI_API_KEY`
  - `SUMMARY_AI_API_KEY`
  - `FORECAST_AI_API_KEY`
- 推荐分层：
  - 默认远端主线保持 `DeepSeek`
  - 当需要双 AI 分工时，再增量开启 stage-level routing

### P3：运营与交付面升级

目标：让 VS_AI 从“有输出”变成“好管理、好追踪、好迭代”。

工作包：

1. 强化 ops dashboard
   - 来源故障排行榜
   - 渠道稳定性趋势
   - topic 覆盖趋势
   - prompt / model 成本趋势
2. 强化 review queue 优先级策略
3. P3-A：飞书推送界面优化
   - 先做静态卡片模板
   - 保留文本消息 fallback
   - 不与 LiteLLM / tracing 首轮接入混做
4. P3-B：飞书多维表格运营台
   - `source_governance` -> 主治理表
   - `source_lead_review_status` -> 审批状态表
   - `candidate_updates` -> 待应用变更表
   - `run-status` / delivery audit -> 交付验收表
   - 视图 / 仪表盘 / 自动化
5. 强化 Pages 的长期跟踪页与差异提示
6. 引入 DailyOps 式持续优化动作

当前判断（2026-04-23）：

- [x] `P3-A` 的静态卡片模板与文本 fallback 已完成真实飞书验收
- [x] `P3-A` 的 Feishu-only 验证命令已补齐（`diagnose-delivery --mode full-report --send --channels feishu`）
- [x] `2026-04-22` 上一轮远端 `Collect And Report` 已全链路通过（run `24762469538` / commit `46c47ef`）
- [x] `P3-A` 的本地卡片主路径验证已补齐：`diagnose-delivery --mode full-report --send --channels feishu` 返回 `delivery_kind=card_success`
- [x] `P3-A` 的 delivery observability 已补齐：`run-status` / diagnose 已区分 `card_success / text_fallback`
- [x] `2026-04-23` 最新远端 `Collect And Report` 已成功：run `24817834003` / commit `ce3b090`
- [x] `2026-04-23` `Source Reachability Canary` 已修复并成功：run `24817844639`
- [x] `2026-04-23` `P3-A` 的持续 canary 已收口到远端飞书卡片主路径：`Delivery Canary` 已成功执行 `full-report + feishu + require-feishu-card-success`（run `24826780399` / commit `e67f36f`）
- [x] `P3-B` 的第一版运营台已落地：`VS_AI 今日运营台` + 四张表 + 状态回写已可用
- [x] `P3-B` 的第二轮 polish 已阶段性收口：旧版英文资源清理已有代码与测试闭环；轻自动化本阶段不新增，继续保留 `sync-feishu-ops-desk` / `pull-feishu-ops-status` 手动闭环
- [x] `P3-C` 的国内外对比分析 A 方案已完成：`comparison_brief` 已进入 reviewed 轨样例、长报告和交付通知，本地发布级验证链已通过；下一步讨论公开站优化方案

### P3-C：国内外对比分析（A 方案，新增）

目标：让 VS_AI 不只给出“发生了什么”，还要稳定回答“国内外各自怎么推进、谁更快、差在哪、下一步该盯什么”。

第一阶段只做 A 方案：

- **P3-C-1：source 标签层（已完成）**
  - [x] 为 source 补 `region_scope / org_origin / tech_track / comparison_priority`
  - [x] 先覆盖 5 条主赛道：`frontier-ai / fpga / embedded / personal-hpc / compute-infra`
  - [x] 标签进入 `source_registry` / 运行态 JSON，并有测试保护
- **P3-C-2：comparison brief 与交付消费**
  - [x] 在 summary payload 中新增 `comparison_brief.cn_highlights / intl_highlights / head_to_head / gaps / watchpoints`
  - [x] 长报告展开完整对比，推送与飞书卡片先只消费 `head_to_head` 前 1~2 条
  - [x] 保持中文输出约束与现有 AI fallback，不把事件级配对混进本阶段
- **P3-C-3：验收、文档、发布收口**
  - [x] 补齐本地验证链、报告样例、交接文档与运行态说明
  - [x] 完成一次 reviewed 轨本地验收：`AUTO_PUSH_ENABLED=false`，`stage_status=ok`，`comparison_brief` 在 reviewed JSON / Markdown 中可见
  - [x] P3-C 完成后再进入公开站整理 / 优化；公开站 `https://wsysgz.github.io/VS_AI/` 的具体优化方案必须先讨论确认，未确认前不改 Pages 结构

执行纪律：P3-C 三阶段已按顺序完成；后续不要回头重做 P3-C-1 / P3-C-2，也不要在公开站方案未确认前改 Pages 结构。

后续再逐步吸收 C 方案：事件级国内外配对与对照。

### P4：V1.5 / V2 候选

- Dagster 编排
- collector / analyzer / publisher 拆服务
- 状态入库（SQLite / Postgres）
- 更强的多频道、多租户能力

## 6. 推荐路线对比

### 方案 A：保守修补

只做：

- 继续手工修 selector
- 继续补个别来源
- 补一点 delivery retry

优点：

- 改动小

缺点：

- 来源脆弱性不会本质改善
- 后面仍会反复返工

### 方案 B：均衡偏激进（推荐）

核心思路：

- 保留现有 VS_AI 主链路
- 外挂来源侧车 + AI 网关 + tracing
- 优先把脆弱来源迁到更稳的入口

即：

```text
Current VS_AI core
+ RSSHub sidecar
+ changedetection sidecar
+ selective Firecrawl fallback
+ LiteLLM gateway
+ Langfuse tracing / evals
```

这是当前最值得执行的路线，因为它见效快、可回滚、和现有工程兼容。

### 方案 C：全面重构

上来就：

- 改成 Dagster orchestration
- 拆 collector / analyzer / publisher service
- 上数据库和任务队列

风险：

- 当前收益 / 成本比不高
- 很容易把注意力从“来源稳定性”和“可观测性”转移走

## 7. 外部项目整合建议

### 适合优先落地的

- RSSHub
  - 用作脆弱网站的稳定 feed 替代
- changedetection.io
  - 用作无 feed 站点的旁路监控
- LiteLLM
  - 用作统一 AI Gateway
- Langfuse
  - 用作 tracing / prompt 管理 / 成本与延迟观测

### 适合选择性使用的

- Firecrawl
  - 只给极难抓取的高价值站点做 fallback extractor
- OpenCLI
  - 更适合做来源侧车 / 运维侧车，尤其是无 feed、强 JS、需要浏览器登录态的复杂站点

### 适合借鉴方法但暂不直接接入的

- CLI-Anything
  - 适合作为 adapter registry / agent-native CLI 组织方式的参考
  - 当前不建议直接并入 VS_AI 主链，避免把项目重心从情报流水线转移到工具生态搭建

### 适合后置评估的

- Dagster
  - 适合 V1.5 / V2，而不是现在立刻迁移

## 8. 风险与回滚原则

### 风险

- 新侧车组件会增加部署复杂度
- 来源替换会带来短期覆盖波动
- LiteLLM / Langfuse 接入初期会增加配置量

### 回滚原则

- 所有新增组件优先 sidecar 化，不要直接替换主链路
- 先做配置级切换，不做一步式大重构
- 每个阶段都要能单独验证、单独回滚

## 9. 对 CLI-Anything / OpenCLI 的评估结论

### CLI-Anything

- [x] 已完成评估
- [x] 结论：当前阶段不建议直接引入主链
- [x] 价值：适合作为后续 adapter registry、skill packaging、agent-native tool catalog 的组织参考
- [ ] 当前不作为 P2 直接落地项

### OpenCLI

- [x] 已完成评估
- [x] 结论：比 CLI-Anything 更适合 VS_AI 当前阶段
- [x] 价值：适合作为来源侧车 / 运维侧车，用于浏览器登录态、复杂 JS 页面、可重复的 deterministic 抓取/验证
- [ ] 当前不进入主采集主链
- [ ] 建议作为 `P2.5 / P3` 的试点项，先选一个最难但高价值的动态来源做 pilot

## 10. 下一阶段建议顺序

推荐按这个顺序继续推进：

1. **P3-A：飞书推送界面优化尾部小修**
   - `Delivery Canary` 已升级为飞书卡片主路径 canary（已完成）
   - `card_success / text_fallback` 可观测性（已完成）
   - 标题 / 复核信息 / 信息密度小修
2. **P3-B：飞书多维表格运营台**
   - 阶段性收口已完成
   - 后续只做小修与回归验证，不再新增轻自动化
3. **P3-C：国内外对比分析（A 方案，已完成）**
   - `P3-C-1`：source 标签层（已完成）
   - `P3-C-2`：comparison brief 与交付消费（已完成）
   - `P3-C-3`：验收、文档、发布收口（已完成）
4. **公开站整理 / 优化（下一步先讨论方案）**
   - 目标站点：`https://wsysgz.github.io/VS_AI/`
   - 具体优化方式待讨论确定后再实施
5. **P1：运营尾项治理（只保留小尾项）**
   - 持续观察 `openvino-blog / huggingface-blog / st-blog` 的连通性噪音
6. **P2.5：OpenCLI source / ops sidecar pilot**
7. **P3：dashboard / review queue / Pages 升级**
8. **DailyOps 自动改良机制**
9. **Dagster / 服务化**

执行原则补充（2026-04-19）：

- 尽量按阶段整段推进，不在多个阶段之间来回磨
- 当前阶段未完成本地代码、文档、验证闭环前，不打开下一阶段实现
- 后续阶段的新想法先写入路线图，不在当前阶段实现中顺手混做

## 11. 外部链接汇总

- [RSSHub](https://docs.rsshub.app/joinus/)
- [changedetection.io](https://github.com/dgtlmoon/changedetection.io)
- [Firecrawl](https://docs.firecrawl.dev/features/crawl)
- [LiteLLM OSS](https://www.litellm.ai/oss)
- [Langfuse Docs](https://langfuse.com/docs)
- [CLI-Anything](https://github.com/HKUDS/CLI-Anything)
- [OpenCLI Docs](https://opencli.info/docs/)
- [OpenCLI Plugins](https://opencli.info/docs/guide/plugins.html)
- [Dagster](https://dagster.io/)
- [finaldie/auto-news](https://github.com/finaldie/auto-news)
- [4uffin/auto-news-aggregator](https://github.com/4uffin/auto-news-aggregator)
- [Ansub/agno-ai-news-slack-bot](https://github.com/Ansub/agno-ai-news-slack-bot)
- [GitHub Agentic Workflows DailyOps](https://github.github.com/gh-aw/patterns/daily-ops/)
