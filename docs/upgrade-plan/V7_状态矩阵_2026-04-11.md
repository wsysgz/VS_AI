# VS_AI V7 状态矩阵

> 更新时间：2026-04-12
> 口径：以当前工作区已存在且可验证的本地实现为准，状态仅分为 `done / in progress / not started`

## 验证基线

- `$env:PYTHONPATH='src'; python -m pytest tests -q` → 当前基线 `190 passed`
- `pwsh ./scripts/check-workflows.ps1 -Profile full`
- `$env:PYTHONPATH='src'; python -m auto_report.cli evaluate-prompts --dataset config/prompt_eval/baseline-v1.json`
- `$env:PYTHONPATH='src'; python -m auto_report.cli run-once --publication-mode reviewed`
- `$env:PYTHONPATH='src'; python -m auto_report.cli build-pages`
- `$env:PYTHONPATH='src'; python -m auto_report.cli build-ops-dashboard`

## Phase 6: Workflow Engineering

| 项目 | 状态 | 证据 | 剩余缺口 |
|------|------|------|----------|
| reusable workflows 串接主流程 | `done` | `.github/workflows/collect-report.yml`、`.github/workflows/reusable-*.yml`、`tests/test_workflows.py` | 继续保持复用边界稳定 |
| workflow guard 接入 `actionlint + act + check-workflows.ps1` | `done` | `.github/workflows/reusable-workflow-guard.yml`、`scripts/check-workflows.ps1`、`tests/test_workflow_guard.py` | 本地快验文档还可继续细化 |
| Python 验证矩阵与 `max-parallel` | `done` | `.github/workflows/reusable-python-test.yml`、`tests/test_workflows.py` | 如需更强覆盖，可再引入更细的 workflow profile |
| 调度错峰 + 失败补偿 workflow | `done` | `.github/workflows/collect-report.yml`、`.github/workflows/compensate-report.yml` | 继续观察远端调度稳定性 |
| `act` 本地 profile 与统一验收入口 | `done` | `scripts/check-workflows.ps1`、`src/auto_report/workflow_guard.py`、`tests/test_workflow_guard.py`、`docs/OPS_RUNBOOK.md` | 已固定为 `daily / recovery / full` 三档 |

## Phase 7: Delivery Reliability

| 项目 | 状态 | 证据 | 剩余缺口 |
|------|------|------|----------|
| `diagnose-delivery` canary / full-report 模式 | `done` | `src/auto_report/cli.py`、`src/auto_report/app.py`、`tests/test_cli_smoke.py` | 可继续补远端演练记录 |
| 通道错误分类与 `run-status.json` 送达摘要 | `done` | `src/auto_report/integrations/delivery_status.py`、`src/auto_report/pipeline/run_once.py`、`tests/test_delivery_status.py` | 可继续扩展更细错误文案 |
| reliability issue：全通道失败 / 连续 canary 失败 / 高风险报告 | `done` | `.github/workflows/collect-report.yml`、`.github/workflows/delivery-canary.yml`、`.github/workflows/compensate-report.yml`、`tests/test_workflows.py` | 后续只需根据运营经验微调标题与标签 |
| issue body 携带 risk/scheduler/stage/channel 摘要 | `done` | `.github/workflows/collect-report.yml` | 仍可增加更多排障链接 |

## Phase 8: PromptOps & EvalOps

| 项目 | 状态 | 证据 | 剩余缺口 |
|------|------|------|----------|
| 版本化 prompt registry + legacy prompt 兼容 | `done` | `src/auto_report/pipeline/prompt_loader.py`、`tests/test_prompt_loader.py` | 后续只需增加 prompt 清单治理 |
| `evaluate-prompts --dataset` 离线回归 | `done` | `src/auto_report/pipeline/prompt_evaluator.py`、`src/auto_report/cli.py`、`tests/test_prompt_evaluator.py` | 已固定到正式 dataset 路径 |
| prompt eval 历史趋势进入私有 ops dashboard | `done` | `src/auto_report/outputs/ops_dashboard.py`、`tests/test_ops_dashboard.py` | 可继续加更细归因，但主链路已完成 |
| delivery / fallback / 风险观测收口到 dashboard | `done` | `src/auto_report/outputs/ops_dashboard.py`、`src/auto_report/pipeline/run_once.py`、`tests/test_ops_dashboard.py` | `ai_metrics`、`source_health`、`review` 已进入统一 schema |
| 正式 benchmark dataset 治理 | `done` | `config/prompt_eval/baseline-v1.json`、`config/prompt_eval/README.md`、`config/ai_reading/registry.json` | 进入维护期 |

## Phase 9: Intelligence Layer

| 项目 | 状态 | 证据 | 剩余缺口 |
|------|------|------|----------|
| 跨日主线记忆、生命周期、`risk_level`、support evidence | `done` | `src/auto_report/pipeline/intelligence.py`、`src/auto_report/pipeline/analysis.py`、`tests/test_intelligence.py`、`tests/test_analysis.py` | 继续观察规则阈值是否需要收敛 |
| 高价值主题外部二次 enrichment | `done` | `src/auto_report/pipeline/intelligence.py`、`src/auto_report/pipeline/analysis.py`、`src/auto_report/outputs/ops_dashboard.py`、`tests/test_intelligence.py`、`tests/test_analysis.py`、`tests/test_ops_dashboard.py` | 已补齐 `official / repo / paper` budget 与命中统计 |
| 人工复核 Issue 化工作流 | `done` | `src/auto_report/pipeline/review_queue.py`、`src/auto_report/cli.py`、`.github/workflows/reusable-review-queue.yml`、`.github/workflows/collect-report.yml`、`tests/test_review_queue.py`、`tests/test_workflows.py` | 后续只需根据运营经验微调筛选阈值与标签策略 |
| 生命周期升级 / 降级规则硬化 | `done` | `src/auto_report/pipeline/intelligence.py`、`tests/test_intelligence.py` | 已加入带滞后的状态机，降低单次波动直接打回 `fading` 的概率 |

## Phase 10: Publishing Surface

| 项目 | 状态 | 证据 | 剩余缺口 |
|------|------|------|----------|
| Pages 首页、archives、search index、JSON Feed、RSS | `done` | `src/auto_report/outputs/pages_builder.py`、`docs/index.html`、`docs/search-index.json`、`docs/feed.json`、`docs/rss.xml`、`tests/test_pages_builder.py` | 继续保持公开面产物稳定 |
| 周报 / 专题页 | `done` | `src/auto_report/outputs/pages_builder.py`、`tests/test_pages_builder.py`、`docs/weekly/`、`docs/special/` | 已补上 weekly“持续主线”和 `emerging` 专题 |
| 渠道模板层（微信 / Telegram / Feishu） | `done` | `src/auto_report/outputs/renderers.py`、`src/auto_report/app.py`、`tests/test_renderers.py`、`tests/test_run_once.py` | 已固定为 PushPlus `short`、Feishu `medium`、Telegram `long`，三端统一双链接 |
| `auto` / `reviewed` 双轨发布 | `done` | `src/auto_report/app.py`、`src/auto_report/pipeline/run_once.py`、`src/auto_report/outputs/pages_builder.py`、`src/auto_report/cli.py`、`.github/workflows/collect-report.yml`、`.github/workflows/reusable-report.yml`、`.github/workflows/backfill-report.yml`、`.github/workflows/reusable-backfill.yml`、`tests/test_run_once.py`、`tests/test_pages_builder.py`、`tests/test_workflows.py` | reviewer metadata / 审核备注 / Pages review badge / 双链接策略已收口 |

## 进入维护观察

1. 继续观察 workflow `daily / recovery / full` 三档是否需要再细分，但当前验收链已固定。
2. 继续观察 `baseline-v1` 的样例覆盖度与 prompt 回归趋势，但 PromptOps 主链路已完成。
3. 继续观察 enrichment 命中率与专题策展表达，但本轮 V7 收口项已全部完成。

## 2026-04-12 实跑验收补充

- 线下：`$env:PYTHONPATH='src'; python -m auto_report.cli run-once --publication-mode reviewed` 已真实执行，`run-status.json` 记录三端送达成功，review queue 产出 1 条候选 issue。
- 线上：`Collect And Report` 手动运行 `24289432310` 已真实执行，但在 `workflow-guard` 被 `actionlint` 以 `SC2015` 拦下；说明 workflow engineering 虽已成型，仍需持续用“远端真跑”校验细节。
- 已完成的本地修复：`reusable-pages.yml` 改为同步当前触发分支、用显式 `if` 代替 `A && B || C`、提交完整 Pages 产物；`collect-report.yml` 的 `reliability-issue` / `review-issue` 仅在上游 artifact 存在时运行；`reusable-backfill.yml` 与 `compensate-report.yml` 也已补齐完整 Pages 回写范围。
- 当前仍待远端复验的点：修复后的 workflow 需要在允许推送后再次上线实跑，才能把 Phase 6 / Phase 10 的“真实线上验收”结论正式收口。
- 本轮已完成的内容质量收口包括：`source_stats.report_topics` 命名统一、公开 `limitations` 与 collector diagnostics 分离、来源异常统一进入 `source_health` 分类展示。
