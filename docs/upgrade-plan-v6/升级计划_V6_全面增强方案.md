# VS_AI 项目 V6 全面升级计划

> **编制日期**: 2026-04-11
> **编制依据**: 参考资源与优化方案.md + 5个同类项目分析(Horizon/DailyTechNews等) + GitHub Actions 最佳实践 + 代码深度审计
> **核心约束**: 每日 GitHub Actions 自动运行时间 **≤ 60 分钟**
> **当前基线**: 单 job 顺序执行, timeout=25min(已非常紧张), 正常运行约 20-40min

---

## 一、项目现状深度诊断

### 1.1 已完成的五轮升级

| 轮次           | 核心内容                                                               | 状态 |
| -------------- | ---------------------------------------------------------------------- | ---- |
| R1 基础架构    | AI预读文件×3 + 三阶段管线(analysis/summary/forecast) + 本地验证链路   | ✅   |
| R2 表现层      | Executive Briefing适配 + 微信30s摘要/TG完整简报/MD归档 三种分层        | ✅   |
| R3 代码补全    | backfill命令 + HTML深色科技风渲染器 + 测试46→51个 + Edge Infra源包9个 | ✅   |
| R4 扩源+AI增强 | +7源(共20+入口,74条原始信息) + 评分模型升级 + Fallback优化             | ✅   |
| R5 飞书+安全   | 飞书推送适配器 + .env密钥存储 + 推送容错(三通道try/except)             | ✅   |

### 1.2 当前架构全景

```
cli.py ──→ app.py.run_once()
            ├── collect_all_items()          ← 采集层（串行！）
            │   ├── _collect_rss()           ← 5个RSS源 逐个fetch
            │   ├── _collect_github()        ← 3组9个仓库 逐个API调用
            │   └── _collect_websites()      ← 12个网站 逐个爬取
            ├── build_report_package()       ← 去重→分类→评分→topic构建
            │   └── run_staged_ai_pipeline() ← AI管线（串行Analysis！）
            │       ├── for candidate: deepseek()  ← N次串行调用!
            │       ├── deepseek()           ← Summary汇总
            │       └── deepseek()           ← Forecast预测
            ├── render_reports()             ← MD+JSON+HTML三格式输出
            └── 推送                        ← PushPlus + Telegram + 飞书
```

### 1.3 🔴 瓶颈诊断（耗时分解）

基于代码审计的实际耗时估算：

| 阶段                     | 代码位置                           | 耗时估算           | 占比             | 问题                                     |
| ------------------------ | ---------------------------------- | ------------------ | ---------------- | ---------------------------------------- |
| **采集-RSS**       | `collector.py:_collect_rss`      | 30-60s             | 3%               | 5源×10s, 可接受                         |
| **采集-GitHub**    | `collector.py:_collect_github`   | **2-5min**   | **15%**    | ⚠️**9仓串行**×20s超时           |
| **采集-Website**   | `collector.py:_collect_websites` | **1-3min**   | **10%**    | ⚠️**12源串行**×20s超时          |
| **去重+分类+评分** | pipeline/*.py                      | <10s               | <1%              | 内存操作，无问题                         |
| **AI-Analysis**    | `ai_pipeline.py:196` for循环     | **10-25min** | **50-65%** | 🔴**N候选×20-40s串行 (最大瓶颈)** |
| **AI-Summary**     | `ai_pipeline.py:215`             | 1-3min             | 7%               | 单次调用，可接受                         |
| **AI-Forecast**    | `ai_pipeline.py:229`             | 1-3min             | 7%               | 单次调用，可接受                         |
| **渲染输出**       | `renderers.py`                   | <5s                | <1%              | 无问题                                   |
| **推送×3通道**    | `app.py:233-266`                 | 30s-2min           | 4%               | TG可能超时(已有容错)                     |
| **Git提交**        | workflow手写git                    | 30-60s             | 1%               | 可替换为action                           |
| **总计**           |                                    | **18-40min** | 100%             | **timeout仅25min! 非常危险**       |

### 1.4 关键发现

1. **AI Analysis 串行调用是绝对最大瓶颈** — 占总耗时 55%+, 波动最大(取决于候选数量)
2. **GitHub Actions timeout=25min 不够用** — 正常情况就可能超时，必须拆分 job
3. **采集层全部串行** — RSS/GitHub/Website 三大类之间及内部都无并发
4. **无执行计时** — 不知道每个阶段到底花了多久，无法精准优化
5. **单模型依赖 DeepSeek** — API故障则全线降级到规则fallback

---

## 二、V6 升级路线图总览

```
Phase 0: 工程加固 & CI 重构     ← 最优先, 直接解决超时风险 (2-3h)
Phase 1: AI Pipeline 性能优化    ← 最大收益点, 解决50%+耗时 (半天)
Phase 2: GitHub Pages 在线快报   ← 产品体验升级 (2-3h)
Phase 3: 能力增强               ← 数据源/多模型/告警 (1-2天)
Phase 4: 可观测性 & 运维         ← 监控/测试可视化 (半天)
Phase 5: 进阶功能(MCP/邮件等)    ← 未来考虑
```

---

## 三、Phase 0: 工程加固 & CI 重构 ⚡ 最高优先级

**目标**: 将 Actions 运行时间稳定在 30-45min, 总上限 ≤ 60min
**预计工作量**: 2-3小时
**预期收益**: 彻底消除超时风险 + 采集加速3-5x

### 3.1 Workflow Job 拆分（核心改动）

**现状问题**: 所有步骤在一个 job 中顺序执行, timeout=25min 已经是极限
**新设计**: 3个独立 job, 各设独立 timeout

```yaml
# .github/workflows/collect-report.yml (重构后)

name: Collect And Report V6

on:
  push:
    branches: [main]
    paths-ignore: ["data/**"]
  workflow_dispatch:
    inputs:
      push_enabled:
        description: "Send notifications"
        required: false
        default: false
        type: boolean
  schedule:
    - cron: "0 23 * * *"  # 北京时间 07:00

concurrency:
  group: collect-report-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ════════════════════════════════════════
  # JOB 1: 采集 + 预处理 (无AI)
  # ════════════════════════════════════════
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/pip
            .venv
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -e .

      - name: Collect data
        timeout-minutes: 12
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          AUTO_TIMEZONE: Asia/Shanghai
        run: python -m auto_report.cli collect-only   # 新子命令

      - name: Upload collected data
        uses: actions/upload-artifact@v4
        with:
          name: collected-data
          path: |
            data/reports/
            data/state/
          retention-days: 1

  # ════════════════════════════════════════
  # JOB 2: AI 分析 (最耗时)
  # ════════════════════════════════════════
  analyze:
    needs: collect
    runs-on: ubuntu-latest
    timeout-minutes: 35              # AI分析给足35分钟
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt', 'pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install
        run: pip install -r requirements.txt && pip install -e .

      - name: Download collected data
        uses: actions/download-artifact@v4
        with:
          name: collected-data
          path: data/

      - name: Run AI analysis
        timeout-minutes: 32
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          AI_MODEL: deepseek-chat
          AI_TEMPERATURE: "0.2"
        run: python -m auto_report.cli analyze-only   # 新子命令

      - name: Upload analyzed data
        uses: actions/upload-artifact@v4
        with:
          name: analyzed-data
          path: data/reports/
          retention-days: 1

  # ════════════════════════════════════════
  # JOB 3: 渲染 + 推送 + 归档
  # ════════════════════════════════════════
  report:
    needs: analyze
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: pip install -r requirements.txt && pip install -e .

      - name: Download analyzed data
        uses: actions/download-artifact@v4
        with:
          name: analyzed-data
          path: data/

      - name: Render and push
        timeout-minutes: 8
        env:
          PUSHPLUS_TOKEN: ${{ secrets.PUSHPLUS_TOKEN }}
          PUSHPLUS_CHANNEL: clawbot
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}
          FEISHU_CHAT_ID: ${{ secrets.FEISHU_CHAT_ID }}
          REPORT_REPO_URL: https://github.com/wsysgz/VS_AI
          AUTO_TIMEZONE: Asia/Shanghai
          AUTO_PUSH_ENABLED: ${{ github.event_name == 'schedule' || inputs.push_enabled == true }}
        run: python -m auto_report.cli render-and-push   # 新子命令

      - name: Auto commit
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "chore: update auto reports [skip ci]"
          file_pattern: "data/**"
          commit_user_name: github-actions[bot]
          commit_user_email: 41898282+github-actions[bot]@users.noreply.github.com

      - name: Upload final artifacts
        uses: actions/upload-artifact@v4
        with:
          name: final-report
          path: data/
```

**关键收益对比**:

| 维度         | 旧(V5)                     | 新(V6 Phase 0)                |
| ------------ | -------------------------- | ----------------------------- |
| Job 数量     | 1个                        | **3个独立job**          |
| Timeout 上限 | 25min (**经常不够**) | **15+35+10 = 60min** ✅ |
| 失败隔离     | 任一步骤失败全部重跑       | 仅重试失败的job               |
| Git 操作     | 手写4行bash                | git-auto-commit-action        |
| 采集方式     | 全串行                     | 并发化(见3.3节)               |

### 3.2 CLI 子命令拆分

**文件**: `cli.py` / `app.py`

新增三个 CI 专用子命令，将现有 `run_once` 拆为三个可独立执行的阶段：

```
现有命令（保留不动）：
  run-once          → 完整流程（本地开发/调试用）
  backfill          → 补报
  render-report     → 渲染报告

新增命令（CI专用）：
  collect-only      → Job1: 采集→去重→分类→评分→保存中间结果
  analyze-only      → Job2: 读取中间结果→AI三阶段分析→保存分析结果
  render-and-push   → Job3: 渲染MD+JSON+HTML→三通道推送→归档
```

数据流转方式：通过 JSON 中间文件在 job 间传递

```
Job1(collect)  → data/state/intermediate.json  → 上传 artifact
Job2(analyze)  → 下载artifact → data/state/analysis-result.json → 上传 artifact
Job3(report)   → 下载artifact → 渲染→推送→commit
```

### 3.3 采集层并发化

**文件**: `sources/collector.py`
**技术**: `concurrent.futures.ThreadPoolExecutor`

改动清单：

1. **三大类采集器并行启动** (RSS / GitHub / Website 同时跑)
2. **GitHub 内部仓库并行** (9个仓库同时 API 调用)
3. **Website 内部并行** (12个网站同时抓取)
4. **全局超时控制** (整个采集不超过120秒)

核心改造：

```python
# collector.py 重构后
import concurrent.futures

def collect_all_items(settings):
    all_items = []
    diagnostics = []

    # 三大类采集器并行启动
    with concurrent.futures.ThreadPoolExecutor(max_workers=3) as executor:
        future_to_name = {
            executor.submit(_collect_rss, settings): "rss",
            executor.submit(_collect_github, settings): "github",
            executor.submit(_collect_websites, settings): "websites",
        }
        for future in concurrent.futures.as_completed(future_to_name, timeout=120):
            name = future_to_name[future]
            try:
                items, msgs = future.result()
                all_items.extend(items)
                diagnostics.extend(msgs)
            except Exception as exc:
                diagnostics.append(f"{name} collection failed: {exc}")

    return all_items, diagnostics


# _collect_github 内部也并发化:
def _collect_github(settings):
    # ... 初始化 ...
    def _fetch_one_repo(full_name):
        resp = requests.get(f"https://api.github.com/repos/{full_name}", timeout=20, headers=headers)
        resp.raise_for_status()
        return normalize_github_repository_detail(...)

    with concurrent.futures.ThreadPoolExecutor(max_workers=5) as executor:
        futures = {executor.submit(_fetch_one_repo, repo): repo for repo in repositories}
        for future in concurrent.futures.as_completed(futures, timeout=60):
            try:
                item = future.result()
                if item: items.append(item)
            except Exception as exc:
                diagnostics.append(f"GitHub repo failed: {exc}")

# _collect_websites 同理加 ThreadPoolExecutor(max_workers=6)
```

**性能预估**：

| 场景             | 旧(全串行) | 新(并发化)            | 加速比         |
| ---------------- | ---------- | --------------------- | -------------- |
| 采集总计         | 3-8 min    | **1-2.5 min**   | **3-4x** |
| GitHub API (9仓) | 2-5 min    | **0.5-1 min**   | ~5x            |
| Website (12源)   | 1-3 min    | **0.3-0.8 min** | ~4x            |

### 3.4 其他工程加固项

| # | 改动                               | 文件         | 收益                            | 工作量  |
| - | ---------------------------------- | ------------ | ------------------------------- | ------- |
| 1 | git-auto-commit-action 替换手写git | workflow.yml | 更可靠 +`[skip ci]`防循环触发 | 5min    |
| 2 | 浅克隆 `fetch-depth: 1`          | workflow.yml | 省去拉取git历史的时间           | 1行     |
| 3 | pip 缓存 restore-keys 回退         | workflow.yml | 缓存未命中时部分复用            | 3行     |
| 4 | 各 step 显式 timeout               | workflow.yml | 防止单步死等拖垮整个job         | 每个1行 |

---

## 四、Phase 1: AI Pipeline 性能优化 🚀 最大收益

**目标**: 将 AI Analysis 阶段从 10-25min 降到 3-8min
**预计工作量**: 半天
**预期收益**: 总运行时间降低 40-60%

### 4.1 并行 Analysis 调用（核心优化）

**文件**: `pipeline/ai_pipeline.py`
**现状**: 第196行 `for candidate in selected_candidates:` 逐个同步调 DeepSeek
**改动**: `ThreadPoolExecutor` 并行，最多5路同时调用

```python
from concurrent.futures import ThreadPoolExecutor, as_completed

def _analyze_single_candidate(candidate, ai_readings):
    """单个候选的AI分析（线程安全，可被并发调用）"""
    prompt = _build_analysis_prompt(ai_readings["analysis"], candidate)
    raw = summarize_with_deepseek(prompt)
    parsed = _parse_json_block(raw)
    parsed = _unwrap_named_payload(parsed, "analysis")
    parsed = _validate_required_keys(parsed, ANALYSIS_REQUIRED_KEYS)
    parsed.setdefault("title", candidate.title)
    parsed.setdefault("url", candidate.url)
    parsed.setdefault("primary_domain", candidate.primary_domain)
    return parsed


# 替换 run_staged_ai_pipeline 中的原有 for 循环:
if ai_enabled:
    max_workers = min(len(selected_candidates), 5)  # 最多5路并发
    analyses = []

    with ThreadPoolExecutor(max_workers=max_workers) as executor:
        future_to_candidate = {
            executor.submit(_analyze_single_candidate, c, ai_readings): c
            for c in selected_candidates
        }
        for future in as_completed(future_to_candidate):
            candidate = future_to_candidate[future]
            try:
                analysis = future.result(timeout=60)  # 单个超时60s
                analyses.append(analysis)
            except Exception:
                analyses.append(_fallback_analysis(candidate))  # 独立fallback

    stage_status["analysis"] = "ok"
else:
    analyses = [_fallback_analysis(c) for c in selected_candidates]
    stage_status["analysis"] = "fallback"
```

**性能预估**：

| 候选数 | 旧(串行) | 新(5路并行)      | 节省   |
| ------ | -------- | ---------------- | ------ |
| 5 个   | 100-200s | **20-40s** | 75-80% |
| 8 个   | 160-320s | **32-64s** | 80%    |
| 12 个  | 240-480s | **48-96s** | 80%    |

**注意事项**：

- `max_workers=5` 平衡速率和 DeepSeek rate limit
- 每个线程独立 fallback，一个失败不阻塞其他
- 单个请求 timeout=60s（比原来 40s 宽松一些，因为有 retry）

### 4.2 AI 预筛选（参考 Horizon 的 Score+Filter）

**原理**: 在送去完整三阶段分析前，先用一次轻量 LLM 调用批量打分过滤低质量内容
**新增文件**: `pipeline/scoring_llm.py`

核心逻辑：

```
候选主题(12个) → LLM批量打分(1次调用, ~15s) → 分数 ≥ 5.0 的进入深度分析(约6个)
                                                    ↓ 分数 < 5.0 的走 fallback
```

**集成位置**: `run_staged_ai_pipeline()` 入口处，在 Analysis 循环之前

**收益**：

| 指标                   | 无预筛选                  | 有预筛选                         |
| ---------------------- | ------------------------- | -------------------------------- |
| LLM 总调用次数(12候选) | 14次 (12分析+1汇总+1预测) | ~9次 (1打分+~6分析+1汇总+1预测) |
| Token 消耗             | ~100K tokens              | ~55K tokens (**-45%**)     |
| Analysis 时间          | 160-320s                  | **35-55s** (含打分)        |
| 报告质量               | 含噪音                    | 更聚焦高价值信号                 |

### 4.3 DeepSeek 客户端加固

**文件**: `integrations/deepseek.py`

三项改进：

1. **Session 连接池复用** — TCP 保持连接，减少每次 ~100ms 握手开销
2. **Retry + 指数退避** — 网络抖动自动恢复 (3次重试: 1s→2s→4s)
3. **429 Rate Limit 处理** — 被限流时等待 60s 后重试
4. **递增超时** — 第1次 45s, 第2次 55s, 第3次 65s

### 4.4 Phase 1 收益汇总

| 优化项         | 时间节省                                | Token节省      | 改动复杂度        |
| -------------- | --------------------------------------- | -------------- | ----------------- |
| 并行 Analysis  | **-120~240s**                     | 无变化         | 低 (改一个函数)   |
| AI 预筛选      | **-60~180s** (减少调用数)         | **-45%** | 中 (新增一个文件) |
| 客户端 Retry   | 减少因瞬时错误的重跑                    | 无             | 低                |
| Session 复用   | -5-10s                                  | 无             | 低                |
| **合计** | **总运行从 25-40min → 10-20min** | **-45%** |                   |

---

## 五、Phase 2: GitHub Pages 在线快报站 🌐

**目标**: HTML 快报变成在线访问网站
**预计工作量**: 2-3小时
**参考**: Horizon 的 Deploy → GitHub Pages 模式

### 5.1 Workflow 集成

追加第 4 个 deploy job 到 workflow：

```yaml
  deploy-pages:
    needs: report
    runs-on: ubuntu-latest
    timeout-minutes: 5
    if: github.event_name == 'schedule'  # 仅定时任务部署
    permissions:
      contents: write
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1

      - name: Build site
        run: |
          mkdir -p dist
          cp data/reports/latest-summary.html dist/index.html

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          publish_branch: gh-pages
          force_orphan: true
```

**前置条件**: GitHub 仓库 Settings → Pages → Source 选 `gh-pages` branch `/ (root)`
**产出地址**: `https://wsysgz.github.io/VS_AI/`

### 5.2 HTML 增强迭代方向

| 增强                  | 说明                                  | 优先级 | 工作量 |
| --------------------- | ------------------------------------- | ------ | ------ |
| 历史导航栏            | 列出近7天报告链接(读取archives/)      | P1     | 30min  |
| 移动端适配检查 & 修复 | 手机浏览器体验优化                    | P1     | 1h     |
| 信号趋势图            | Chart.js 绘制近期信号数量趋势         | P2     | 2h     |
| 深色/浅色主题切换     | 尊重系统偏好 `prefers-color-scheme` | P2     | 1h     |

---

## 六、Phase 3: 能力增强 🔧

### 3.1 新增数据源

| 数据源                            | 类型       | 参考项目               | 理由                         | 优先级       |
| --------------------------------- | ---------- | ---------------------- | ---------------------------- | ------------ |
| **Hacker News Top Stories** | JSON API   | Horizon, DailyTechNews | 国际科技社区风向标,零成本API | **P1** |
| **36Kr AI频道**             | RSS/Scrape | 中文AI资讯             | 国内创投+AI动态一手信息      | **P1** |
| **机器之心**                | RSS        | 中文AI专业媒体         | 深度中文AI报道               | P1           |
| **ArXiv cs.AI/cs.CL**       | API        | 学术前沿               | 论文预印本追踪               | P2           |
| **Product Hunt**            | Scrape     | 新产品发现             | AI工具/产品创新趋势          | P3           |

**HN API 实现要点** (最简单, 零额外依赖):

- GET `https://hacker-news.firebaseio.com/v0/topstories.json` → 获取 top story IDs
- 批量 GET `https://hacker-news.firebaseio.com/v0/item/{id}.json` → 获取详情
- 可内部并发抓取 (ThreadPoolExecutor)

### 3.2 多 LLM Provider 抽象

**新增文件**: `integrations/llm_provider.py`
**设计模式**: 策略模式 + 工厂函数

支持的后端（按优先级）：

1. **DeepSeek** (默认, 现有行为不变)
2. **OpenAI 兼容** (GPT-4o-mini / 任何 OpenAI API 兼容服务)

环境变量切换：

```bash
# .env
AI_PROVIDER=deepseek          # 默认
# AI_PROVIDER=openai           # 切换到OpenAI兼容后端
# OPENAI_API_KEY=sk-xxx
# OPENAI_BASE_URL=https://api.openai.com/v1
```

### 3.3 高风险信号 Issue 告警

**场景**: forecast 阶段检测到高风险信号时, 自动创建 GitHub Issue 通知人工复核

**实现方式**: `actions/github-script@v7` + 条件触发

```yaml
  - name: Check risk level
    id: check-risk
    run: echo "level=$(python -c "import json; print(json.load(open('data/state/run-status.json')).get('risk_level','low'))")" >> $GITHUB_OUTPUT

  - name: Create issue on high risk
    if: steps.check-risk.outputs.risk_level == 'high'
    uses: actions/github-script@v7
    with:
      script: |
        github.rest.issues.create({
          owner: context.repo.owner,
          repo: context.repo.repo,
          title: '🔴 高风险信号检测: ' + new Date().toISOString().split('T')[0],
          body: '自动检测到高风险信号，请人工复核。\n\n[查看报告](https://wsysgz.github.io/VS_AI/)'
        })
```

---

## 七、Phase 4: 可观测性 & 运维 📊

### 4.1 执行计时仪表盘

**目标**: 每个阶段记录精确耗时, 写入 run-status.json

```python
class StageTimer:
    """简易阶段计时器"""
    def __init__(self, name: str):
        self.name = name
        self.start = 0
        self.elapsed = 0

    def __enter__(self):
        self.start = time.time()
        return self

    def __exit__(self, *args):
        self.elapsed = time.time() - self.start
```

在 `app.py` 各阶段使用:

```python
timings = {}

with StageTimer("collection") as t:
    items, diagnostics = collect_all_items(settings)
timings["collection"] = round(t.elapsed, 1)

with StageTimer("ai_analysis") as t:
    # ... AI pipeline ...
timings["ai_analysis"] = round(t.elapsed, 1)

# 最终写入 run-status.json
status["timings"] = timings
```

**产出示例** (`run-status.json`):

```json
{
  "timings": {
    "collection": 85.3,
    "dedup_scoring": 0.2,
    "ai_analysis": 245.8,
    "ai_summary": 18.2,
    "ai_forecast": 22.1,
    "rendering": 0.3,
    "push_total": 45.6,
    "total_elapsed": 417.5
  },
  "risk_level": "medium",
  "source_stats": { ... }
}
```

### 4.2 测试可视化 + CI 测试步骤

```yaml
  # 在 collect job 或独立的 test job 中:
  - name: Run tests
    run: pytest tests --junitxml=test-results.xml -q --tb=short

  - name: Publish test results
    uses: dorny/test-results-action@v1
    if: always()
    with:
      files: test-results.xml
```

---

## 八、Phase 5: 进阶功能 (未来考虑)

| 功能                         | 说明                                             | 参考项目           | 工作量 | 优先级 |
| ---------------------------- | ------------------------------------------------ | ------------------ | ------ | ------ |
| **MCP Server**         | 暴露标准接口让 Claude/GPT 等 AI 工具直接调用管线 | Horizon            | 1-2天  | P3     |
| **邮件订阅(SMTP)**     | 自建 Newsletter 系统                             | Horizon            | 半天   | P3     |
| **知识增强(搜索背景)** | 对高分信号网络搜索补充上下文                     | Horizon Enrichment | 1-2天  | P3     |
| **双语报告**           | 中英双语版本                                     | Horizon            | 半天   | P3     |
| **Composite Action**   | 将"JSON→HTML渲染"封装成 Reusable Action         | GitHub高级特性     | 2h     | P3     |

---

## 九、实施计划与时间表

### 第一批（本次实施推荐）— Phase 0 + Phase 1 核心

| 步骤                 | 内容                   | 文件                                                 | 预计时间 | 依赖     |
| -------------------- | ---------------------- | ---------------------------------------------------- | -------- | -------- |
| 0.1                  | Workflow 3-job 拆分    | `.github/workflows/collect-report.yml`             | 30min    | 无       |
| 0.2                  | CLI 三子命令拆分       | `cli.py`, `app.py`                               | 1h       | 无       |
| 0.3                  | 采集层并发化           | `sources/collector.py`                             | 40min    | 无       |
| 0.4                  | git-auto-commit-action | workflow                                             | 10min    | 无       |
| 0.5                  | 缓存+浅克隆优化        | workflow                                             | 10min    | 无       |
| 1.1                  | AI Analysis 并行化     | `pipeline/ai_pipeline.py`                          | 30min    | 无       |
| 1.2                  | DeepSeek 客户端加固    | `integrations/deepseek.py`                         | 20min    | 无       |
| 1.3                  | AI 预筛选模块          | `pipeline/scoring_llm.py`(新) + `ai_pipeline.py` | 1h       | 1.1完成  |
| 0-1验证              | 本地测试全部通过       | `pytest tests -q`                                  | 20min    | 以上全部 |
| **第一批小计** |                        | **~5小时**                                     |          |          |

### 第二批 — Phase 2 + Phase 3

| 步骤                 | 内容                       | 预计时间         |
| -------------------- | -------------------------- | ---------------- |
| 2.1                  | Pages deploy job           | 30min            |
| 2.2                  | HTML 历史导航 + 移动端适配 | 1.5h             |
| 3.1                  | HN 数据源接入              | 1h               |
| 3.2                  | 多 Provider 抽象           | 1.5h             |
| 3.3                  | 高风险 Issue 告警          | 30min            |
| **第二批小计** |                            | **~5小时** |

### 第三批 — Phase 4 + Phase 5

| 步骤 | 内容                    | 预计时间 |
| ---- | ----------------------- | -------- |
| 4.1  | 计时仪表盘              | 30min    |
| 4.2  | 测试可视化              | 20min    |
| 5.x  | 进阶功能(MCP/邮件/双语) | 按需     |

---

## 十、风险与应对

| 风险                              | 影响                    | 概率 | 应对措施                                          |
| --------------------------------- | ----------------------- | ---- | ------------------------------------------------- |
| DeepSeek 并发 rate limit          | 分析失败降级到 fallback | 中   | max_workers=5 + 429自动retry + 每线程独立异常处理 |
| GitHub Actions 免费额度耗尽       | 月度 2000min 可能不够   | 低   | Phase 0 优化后预计每天 ≤ 45min, 月耗 ~1350min    |
| Website 源反爬/结构变更           | 采集失败                | 中   | try/except 容错 + 诊断日志 + 定期维护源配置       |
| HN/新源引入噪音                   | 报告质量下降            | 低   | AI预筛选(≥5分阈值) + 人工review初期输出          |
| Workflow 拆分后 artifact 传递失败 | 后续 job 无法运行       | 低   | GitHub Actions artifact 是成熟机制, 失败率极低    |

---

## 十一、成功指标

### 效率指标

| 指标                            | 当前值(V5)                   | 目标值(V6 Phase0+1)                | 测量方式          |
| ------------------------------- | ---------------------------- | ---------------------------------- | ----------------- |
| **Actions 平均运行时间**  | 20-40min (timeout=25min危险) | **10-20min** (上限60min安全) | workflow 运行记录 |
| **采集阶段耗时**          | 3-8min                       | **1-2.5min**                 | StageTimer        |
| **AI Analysis 耗时**      | 10-25min                     | **3-8min**                   | StageTimer        |
| **超时失败率**            | 偶发(当候选多时)             | **接近0%**                   | workflow 历史     |
| **月度 Actions 分钟消耗** | ~600-1200min                 | **≤ 450min**                | GitHub Usage 页面 |

### 质量指标

| 指标           | 当前值            | 目标值                            |
| -------------- | ----------------- | --------------------------------- |
| 测试通过数     | 51                | ≥ 51 (只增不减)                  |
| 推送通道成功率 | ~90% (TG偶尔超时) | **≥ 98%** (已有容错+retry) |
| 报告信息密度   | 含部分噪音        | **更聚焦**(预筛选过滤)      |

---

*以下为 2026-04-11 基于当前仓库实现状态与外部案例补充的对齐审计与 V7 规划。*

---

## 十二、V6 计划与当前实现对齐审计（2026-04-11）

### 12.1 已基本落地

| 模块 | V6 目标 | 当前状态 |
|------|---------|----------|
| CI 拆分 | `collect` / `analyze` / `report` 多 job | ✅ 已落地，并额外加入 `test` + `deploy-pages` |
| CLI 子命令 | `collect-only` / `analyze-only` / `render-and-push` / `build-pages` | ✅ 已落地 |
| AI Analysis 并行化 | `ai_pipeline.py` 并发分析 | ✅ 已落地 |
| AI 预筛选 | 批量 LLM 打分后再深度分析 | ✅ 已落地 |
| 通用 LLM Provider | DeepSeek + OpenAI 兼容抽象 | ✅ 已落地，并补齐自定义 OpenAI 兼容 key 识别 |
| HN 数据源 | Hacker News 热门故事接入 | ✅ 已落地 |
| Pages 站点 | `docs/index.html` + `docs/archives/` | ✅ 已落地 |
| 可观测性 | `timings` / `stage_status` / `source_stats` | ✅ 已落地 |
| 推送状态可观测 | `delivery_results`、分通道成功/失败 | ✅ 已落地 |
| 送达诊断入口 | `diagnose-delivery` / `--send` | ✅ 已落地 |

### 12.2 已部分落地，但仍需补强

| 模块 | V6 预期 | 当前实际 | 结论 |
|------|---------|----------|------|
| Website 并发采集 | 站点内部并发抓取 | 当前仍以串行为主 | ⚠️ 只完成了“采集器级并行”，未完成“站点级并发” |
| Pages 部署 | V6 文档原设想 `gh-pages` 分支 | 当前工程改为 `main/docs` 提交式部署 | ⚠️ 功能可用，但 V6 文档与工程实现漂移 |
| Backfill 工作流 | 一键补报并回写仓库 | 当前 workflow 只上传 artifact，不自动提交补报结果 | ⚠️ 能跑，但闭环未做完 |
| 送达诊断 | 轻量 probe + 明确失败分类 | 当前已有诊断命令，但缺少 canary 节奏和失败类别细分 | ⚠️ 够用但不够“运维化” |

### 12.3 本轮补齐的接管修复

| 类别 | 问题 | 结果 |
|------|------|------|
| Pages 安全性 | `build-pages` 会清空整个 `docs/`，误删手册和审计资料 | ✅ 已修复为只清理 Pages 生成物 |
| LLM provider | `AI_PROVIDER=openai` 或自定义 provider 时，分析阶段可能被误判为未启用 | ✅ 已修复，按真实 provider key 判断 |
| 评分预筛选 | `scoring_llm` 对 JSON 数组响应解析错误 | ✅ 已修复并补回归测试 |
| 送达判定 | `run-status` 过去更偏向“尝试发送”，不够强调“响应确认为成功” | ✅ 已修复为按通道响应结构判断成功 |
| 运行摘要 | `run-status.json` 未归纳 Feishu `message_id` 等信息 | ✅ 已补齐 Feishu 摘要 |
| 手册/示例配置 | `.env.example` 与若干手册未完整暴露真实可配项 | ✅ 已对齐当前实现 |

### 12.4 V6 中仍未实现的功能

| 模块 | 原计划 | 当前状态 |
|------|---------|----------|
| 高风险信号 Issue 告警 | 根据 `risk_level` 自动开 Issue | ❌ 未实现，`run-status.json` 中也无 `risk_level` |
| Reusable Workflow 抽象 | 拆公共 job 逻辑 | ❌ 未实现 |
| 本地 Workflow 快验工具链 | `act` / `actionlint` / VS Code 联调 | ❌ 工程未集成 |
| Prompt 管理与版本化 | 将 AI 提示词从仓库文件提升到可版本化系统 | ❌ 未实现 |
| 系统化评测 | 数据集、回归评测、线上评估 | ❌ 未实现 |
| 调度补偿机制 | 错峰调度、漏跑补偿、延迟探测 | ❌ 未实现 |

### 12.5 当前最值得优先改进的地方

1. 为 workflow 增加 `actionlint` + `act` 的本地快验链，减少每次改 CI 都要推远端试错。
2. 把共享步骤下沉为 reusable workflows，避免 `collect-report.yml` 与其他 workflow 继续漂移。
3. 给定时任务增加错峰与补偿机制，降低 GitHub Actions 整点高负载导致的延迟/漏跑风险。
4. 增加 `risk_level`、全通道失败告警和自动开 Issue 闭环。
5. 将 prompt 管理、评测与版本追踪从“仓库文件”升级为“可审计系统”。

---

## 十三、V7 升级计划（基于 2026-04-11 案例补充）

### 13.1 外部案例与启发

| 案例 | 来源 | 对 V7 的启发 |
|------|------|--------------|
| LiteLLM Router | LiteLLM 官方文档 | 路由层不应只做 provider 切换，还应具备 fallback、timeout、retry、cooldown 与预算感知能力 |
| Langfuse Prompt Management | Langfuse 官方文档 | Prompt 不应继续硬编码在仓库文件中，应中心化版本管理并解耦部署 |
| Langfuse Evaluation | Langfuse 官方文档 | Prompt 或模型调整后要有可重复的回归检查，而不是靠人工抽样 |
| GitHub Reusable Workflows | GitHub Docs 官方文档 | 采集、分析、Pages、回归测试可以进一步模板化，减少 workflow 漂移 |
| GitHub Matrix Jobs | GitHub Docs 官方文档 | 本地/CI 校验可引入矩阵组合与 `max-parallel` 控制，覆盖更多验证场景 |
| GitHub Scheduled Workflow Notes | GitHub Docs 官方文档 | 07:00 策略需要引入错峰和补偿机制，避免整点高负载延迟甚至丢任务 |
| `act` | `nektos/act` | 本地运行 workflow 可显著减少远端试错成本 |
| `actionlint` | `rhysd/actionlint` | 在真正运行 workflow 前，先做静态语法、表达式与安全检查，能提前拦下红点 |
| arXivTimes | GitHub 开源项目 | 适合借鉴“Issue/评审/长期沉淀”的方式处理高价值信号 |

### 13.2 V7 总目标

V7 不再以“再加几个功能”为核心，而是把 VS_AI 从“能跑的自动早报脚本”提升为“可回归、可审计、可本地快验、可持续扩展的情报操作系统”。

### 13.3 V7 路线图

#### Phase 6: Workflow Engineering

**目标**：把 Actions 从“能跑”提升到“可本地验证、可复用、可观测”。

- 引入 `actionlint` 作为本地和 CI 的第一道 workflow 静态检查。
- 在开发机和手册中补齐 `act` 路径，提供最小可跑的本地 workflow profile。
- 将 `collect-report.yml` 中可复用片段沉淀为 reusable workflows。
- 使用 matrix 跑关键 Python / workflow 验证组合，并用 `max-parallel` 控制资源消耗。
- 将定时任务从整点附近错峰，并增加失败补偿触发。

#### Phase 7: Delivery Reliability

**目标**：让“送到了没”不再靠人工猜。

- 把 `diagnose-delivery` 升级为 canary / full-report 两种模式。
- 为 PushPlus / Telegram / Feishu 增加更细粒度失败分类：认证失败、网络失败、权限失败、格式失败。
- 增加每日合成送达探针，可选择只发极简 canary 到三端。
- 为高风险或全通道失败场景自动创建 GitHub Issue，并附带最近一次 `run-status.json` 摘要。

#### Phase 8: PromptOps & EvalOps

**目标**：把 AI 提示与输出质量从“靠感觉调”升级到“可版本化、可回归评测”。

- 将 `config/ai_reading/*.md` 升级为可版本化 Prompt Registry，支持标签、环境、回滚。
- 建立固定评测数据集，对 summary / forecast / domain briefs 做离线回归评测。
- 记录 prompt 版本与输出质量关联，支持快速定位“哪次 prompt 更新让质量下降”。
- 建立线上评估面板：延迟、token、fallback 率、送达率。

#### Phase 9: Intelligence Layer

**目标**：提升报告质量，而不是只提升产量。

- 对高分主题增加二次 enrichment：官方源、issue、release note、论文/仓库交叉印证。
- 引入跨日主题记忆，识别“持续演进的主线”而不是每天重新开始。
- 增加信号级生命周期：新出现 / 持续升温 / 已验证 / 噪音淘汰。
- 为高价值主题创建人工复核工作流，参考 arXivTimes 的 Issue 化沉淀思路。

#### Phase 10: Publishing Surface

**目标**：把输出从“单日报告”升级为“多界面、多节奏、可订阅”。

- 增加周报与专题页，不再只保留单日报。
- 为 Pages 增加结构化索引页、历史检索、JSON feed / RSS feed。
- 补充面向不同渠道的模板层：微信极简卡片、Telegram 长文、Feishu 短中长三档。
- 增加“人类复核版”与“纯自动版”双轨发布选项。

### 13.4 V7 第一批推荐落地项

| 优先级 | 项目 | 原因 |
|--------|------|------|
| P1 | `actionlint` + `act` 本地快验链 | 直接减少远端试错成本和 CI 红点 |
| P1 | 调度错峰 + 失败补偿 | GitHub 官方明确提示整点高负载延迟/掉队风险 |
| P1 | Reusable workflows | 降低多 workflow 漂移与维护成本 |
| P2 | `risk_level` + 自动开 Issue | 把异常从日志变成工作项 |
| P2 | Prompt 管理与评测基线 | 让 AI 质量演进可控 |
| P3 | 周报 / Feed / 检索页 | 提升使用体验与沉淀价值 |

### 13.5 V7 参考资料

- LiteLLM Router: https://docs.litellm.ai/docs/routing
- Langfuse Prompt Management: https://langfuse.com/docs/prompt-management/overview
- Langfuse Evaluation Overview: https://langfuse.com/docs/evaluation/overview
- GitHub Actions Matrix: https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/run-job-variations
- GitHub Reusable Workflows: https://docs.github.com/en/actions/how-tos/reuse-automations/reuse-workflows
- GitHub Scheduled Workflow Notes: https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows
- `act`: https://github.com/nektos/act
- `actionlint`: https://github.com/rhysd/actionlint
- arXivTimes: https://github.com/arXivTimes/arXivTimes

---

*文档结束。本文件前半部分是 V6 计划，后半部分是 2026-04-11 的实现对齐审计与 V7 续规划。*
