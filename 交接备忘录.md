# 交接备忘录

> 仓库：`D:\GitHub\auto`
>
> 用途：防止会话中断、切人接手、上下文清空后失去方向
>
> 更新时间：2026-04-25（最新 Collect / Delivery Canary / Source Reachability Canary 已检查；DeepSeek V4 路由已切换；P3-B 第二轮 polish 已阶段性收口）

这份备忘录只写“最容易丢的上下文”，不重复整本手册。

## 1. 30 秒状态

- 当前默认直接在 `main` 上工作
- 当前主线：`P3-C 国内外对比分析（A 方案）已完成，下一步先讨论公开站整理 / 优化方案`
- 当前阶段纪律：公开站方案确认前不改 Pages 结构，不回头重做 `P3-C-1 / P3-C-2 / P3-C-3`
- 当前新增收口：`repo-local watch runner / watch registry / candidate-updates / apply-source-updates 已打通`
- 当前新增收口：`infineon-blog / qualcomm-onq 已切到稳定 API 入口`
- 当前新增收口：`P3-A` 已完成首轮发布级验收（飞书静态卡片真实发送成功 + 文本 fallback 保留）
- 当前新增收口：`diagnose-delivery --mode full-report --send --channels feishu` 已支持 Feishu-only 验证
- 当前新增收口：`2026-04-22` 本地真实飞书验证已命中 `delivery_kind=card_success`
- 当前新增收口：`run-status` / delivery diagnose 已显式区分 `card_success / text_fallback`
- 当前新增收口：`2026-04-24` 最新远端 `Collect And Report` 已成功（run `24892691284` / commit `a2f1455`）
- 当前新增收口：`2026-04-24` `Delivery Canary` 已远端成功（run `24864175041`）
- 当前新增收口：`2026-04-24` `Source Reachability Canary` 已远端成功（run `24864003455`）
- 当前新增收口：GitHub Repository Variables 已切到 DeepSeek V4 路由（关键阶段 `pro`，常规阶段 `flash`）
- 当前新增收口：`2026-04-25` 本地真实 `auto` 轨已确认推送关键字段为中文，英文推送根因已在 summary/forecast 约束层修正
- 当前新增收口：`2026-04-25` 真实 `sync-feishu-ops-desk` 已执行，现有飞书表已按第一屏可写字段逻辑重排
- 当前新增收口：`P3-B：飞书多维表格运营台` 第二轮 polish 已阶段性收口，后续只保留小修与回归验证
- 当前新增收口：`P3-C-2` 已完成：summary payload 新增 `comparison_brief`，Markdown 长报告完整展示，PushPlus / Telegram / 飞书文本 / 飞书卡片只消费前 1~2 条同轨对照
- 当前新增收口：`P3-C-3` 已完成：workflow guard、全量 pytest、prompt eval、reviewed 轨 run-once、Pages / ops / governance / review queue 构建均已本地通过
- `2026-04-22` 上一轮远端 `Collect And Report` 已全链路通过（run `24762469538` / commit `46c47ef`）
- `2026-04-24` 当前远端稳定基线已推进到：`a2f1455`
- 当前新增尾项：`renesas-blog` 仍 blocked（403）`
- 当前验证基线：`337 passed`
- 运行态真相文件：`data/state/run-status.json`
- 来源治理真相文件：`out/source-governance/source-governance.json`
- 公开站固定入口：`https://wsysgz.github.io/VS_AI/`
- 手动触发 workflow 前必须先 `push`

## 2. 最近已经做成的事

### PushPlus / ClawBot

- 已完成 `clawbot` 最终送达验证
- 已接入：
  - `PUSHPLUS_SECRETKEY`
  - 开放接口
  - 安全 IP 白名单
- 当前成功判定以这两个值为准：
  - `haveContextToken=1`
  - `delivery_status=2`

### 来源治理基础层

- `source_health` 已支持 per-source breakdown
- `source_health` 已改成只统计 failure diagnostics
- `source_registry` 已统一为共享 builder
- `source_governance` 队列与 JSON artifact 已接通
- `source_governance` 已新增治理优先级队列
- `source_governance` 已新增 discovery-derived leads：
  - `official_feed_leads`
  - `rsshub_leads`
  - `changedetection_leads`
- ops dashboard 已新增 `Source Failure Breakdown`
- ops dashboard 已新增 `Source Registry` 与治理队列表格
- ops dashboard 已新增：
  - `Governance Priority Queue`
  - `Discovery Leads`
  - `Official Feed Leads`
  - `RSSHub Leads`
  - `changedetection Leads`
- `arxiv-cs-ai` 已切到官方 Atom 查询接口

### 第一轮坏源清洗

- `NVIDIA/cuda-cmake` -> `NVIDIA/TensorRT`
- `st-blog` 已稳定迁入官方 feed
- `ti-e2e-blog` 已切到 TI 官方 newsroom JSON 接口
- `arm-news` 已切到官方 feed
- `nvidia-embedded` 已切到官方 feed

### 文档与交接

- 手册主入口已统一在仓库根目录：
  - `README.md`
  - `AI对接手册.md`
  - `用户操作手册.md`
  - `V1升级方案.md`
  - `交接备忘录.md`
- `AI对接手册.md` 已强化为新会话 AI / 新接手者的第一入口
- `用户操作手册.md` 已补充本地验收、推仓、手动触发 GitHub 的流程
- 本轮交接里已再次本地跑通 reviewed 验证；当前精确运行快照以 `data/state/run-status.json` 为准

### P3-A 飞书交付面

- 已完成飞书静态卡片真实发送验证
- 已确认卡片按钮：
  - `公开阅读`
  - `GitHub 原文`
- 已修复卡片正文字面量 `\n` 排版问题
- 已补齐 Feishu-only 验证命令，避免误发 PushPlus / Telegram
- 当前交付面收口状态：
  - `2026-04-22` 本地已确认 `card_success`
  - `run-status` 已显式区分 `card_success / text_fallback`
  - `2026-04-23` 最新远端 `Collect And Report` 已成功
  - `2026-04-23` `Delivery Canary` 已升级为飞书卡片主路径 canary，并已远端成功（run `24826780399` / commit `e67f36f`）

### P3-B 飞书运营台（第一版已落地，第二轮已阶段性收口）

- 已有本地 sidecar 同步能力：
  - 飞书日报文档
  - 来源治理表
  - 来源治理任务列表
- `P3-B` 第一版现状：
  - 已建成 `VS_AI 今日运营台`
  - 已建成四张表：
    - `治理总表`
    - `审批协作`
    - `交付验收`
    - `待应用变更`
  - 已支持两条本地命令：
    - `python -m auto_report.cli sync-feishu-ops-desk`
    - `python -m auto_report.cli pull-feishu-ops-status`
  - 已支持状态类回写：
    - `审批协作.status / note`
    - `交付验收.card_verified / fallback_observed / delivery_note`
  - 已确认“按钮优先”字段：
    - `审批协作.状态` 为单选
    - `交付验收.卡片已确认 / 发现回退` 为复选框
- 当前判断：`P3-B` 已不再只是铺底，已进入“第一版可用 + 第二轮阶段性收口”状态
- `2026-04-22` 上一轮远端 `Collect And Report` 已全部通过，说明当前批次仓库主链改动无新增阻塞
- 第二轮已完成的收口：
  - 中文化已收口：`待审批` 状态文案、视图过滤、旧值 `待处理` 迁移兼容
  - 按钮优先已收口：`审批协作 / 交付验收` 可写字段前置到第一屏
  - 旧版英文资源已收口：旧表 / 旧 dashboard 优先删除，失败则改名为“旧版…（停用）”
  - 轻自动化本阶段不新增，保留 `sync-feishu-ops-desk` / `pull-feishu-ops-status` 手动闭环

### P2-A 远端验证

- 已完成远端 stage-level routing 验证
- 当前确认：
  - `analysis` -> DeepSeek
  - `summary` -> MiniMax-M2.7
  - `forecast` -> DeepSeek
  - `prefilter` -> MiniMax-M2.7
- 远端 workflow 已从“支持配置”进入“已验证分工”

### P2-B LiteLLM Gateway

- 已把 `litellm_proxy` 作为仓库一等公民 provider 接入
- 已补：
  - `config/litellm/litellm-config.example.yaml`
  - `config/litellm/litellm-config.local.example.yaml`
  - `config/litellm/litellm.env.local.example`
  - `scripts/start-litellm-gateway.ps1`
  - `scripts/smoke-litellm-gateway.ps1`
  - `scripts/smoke-run-once-via-litellm.ps1`
- 已完成本地 smoke 验证：
  - `prefilter -> vs-ai-prefilter`
  - `analysis -> vs-ai-analysis`
  - `summary -> vs-ai-summary`
  - `forecast -> vs-ai-forecast`

### P2-C Langfuse tracing

- 已完成 metadata-first first batch
- 已完成本地实接验证
- 已完成远端 `push` + `workflow_dispatch` rollout 验证
- 当前 tracing 覆盖：
  - root trace：`run_once`
  - stage span：`collection / dedup_score / rendering / push_total`
  - AI generation：`prefilter / analysis / summary / forecast`
- 当前默认策略：
  - `LANGFUSE_CAPTURE_CONTENT=false`
  - 默认只传 metadata / stage / provider / model / token / latency / status
  - 默认不上传 prompt / response 正文
- 当前已验证的运行态写回：
  - `tracing.enabled`
  - `tracing.trace_id`
  - `tracing.trace_url`

## 3. 当前还没做完的重点

当前治理尾项已基本收口，不建议继续扩源或并行打开新阶段；默认剩余重点改为：

1. `out/source-governance/changedetection-watch-registry.json` 已切到 repo 内原生 watch runner；当前高优先级 watch 已全部转为 `active_local`
2. `infineon-blog` / `qualcomm-onq` 已切到稳定 API 入口；当前尾项只剩 `renesas-blog`（403）
3. `P3-A` 远端发布级确认已成功；`Delivery Canary` 也已升级为飞书卡片主路径 canary 并远端成功
4. `P3-B` 第二轮 polish 已阶段性收口：中文状态、第一屏可写字段、旧版英文资源处理均已有代码与测试闭环；轻自动化本阶段不新增
5. `2026-04-23` `Source Reachability Canary` 已修复并成功；这条尾项暂时收口
6. `P3-C` 已完成；下一步默认先讨论公开站 `https://wsysgz.github.io/VS_AI/` 的整理 / 优化方案
7. 公开站具体优化方案待讨论确定后再实施，未确认前不改 Pages 结构
8. `P3-B` 只保留小修、真实同步回归和发布前验证
9. `P2.5 OpenCLI pilot` 继续延后，不与当前批次混做

当前治理摘要（仓库现有 artifact）：

- `manual_review_count = 1`
- `rsshub_candidate_count = 4`
- `changedetection_candidate_count = 4`
- `replacement_candidate_count = 5`
- `changedetection_watch_registry.status_counts.active_local = 4`

## 4. 目前最该关注的来源

这些来源已经在当前运行态或治理产物里反复出现，优先级高：

- `nxp-edge-ai`
- `infineon-blog`
- `qualcomm-onq`
- `anthropic-news`（已收口：保留远端验证通过的官方 listing）

判断原则：

- 能切稳定 feed / RSSHub 的，优先切 feed
- 无稳定 feed 但页面重要的，优先建 changedetection watch
- 已经长期失效的，优先替换或保留禁用槽位，不要继续制造噪音
- discovery helper 已经能给出候选建议，但不要把 AI lead 直接当已验证事实

补充一条计划判断：

- `CLI-Anything`：已评估，当前不建议直接接入主链
- `OpenCLI`：已评估，更适合作为来源侧车 / 运维侧车，在 P2.5 / P3 试点

## 5. 用户明确偏好

- 不建功能分支，直接在 `main` 上工作
- Telegram 当前视为网络环境问题，不用花时间修
- 当前主线已经完成 `P3-A`、`P3-B` 阶段性收口与 `P3-C` 国内外对比分析；治理尾项只保留极小 blocked 收口
- `P3-C-1 source 标签层`、`P3-C-2 comparison brief / 交付消费`、`P3-C-3 验收、文档、发布收口` 均已完成
- 下一步才进入公开站整理 / 优化方案讨论；公开站具体怎么优化，必须先讨论确定，不提前改 Pages
- 当前对 P3 的最新判断是：
  - `P3-A`：功能已验通，进入收口
  - `P3-B`：第一版已落地，第二轮 polish 已阶段性收口
- `README.md` 的大规模审美改版不是当前优先级
- 交接文档要服务“上下文清空后快速恢复”，不是做花哨排版

## 6. 当前不要忘的事实

- `run-status.json` 是唯一权威运行态文件
- `AI对接手册.md` 是新会话 AI 的第一入口
- `workflow_dispatch` 只跑远端已 push 代码
- PushPlus 现在看最终送达状态，不看单纯接口成功
- 当前重点不在 Telegram，不在大改架构，而在收尾治理：本地 watch runner、blocked 项收口、发布级验收、交接同步
- 当前新增重点：把“国内外对比分析”正式纳入计划与手册，方便上下文清空后继续推进
- 当前飞书相关命令里，如果只想验证卡片主路径，优先用：
  - `python -m auto_report.cli diagnose-delivery --mode full-report --send --channels feishu --require-feishu-card-success`
- 当前飞书运营台相关命令里，如果要同步 / 回写，优先用：
  - `python -m auto_report.cli sync-feishu-ops-desk`
  - `python -m auto_report.cli pull-feishu-ops-status`
- 当前执行纪律已经明确写入手册：按阶段整段推进，不在多个阶段之间来回磨
- 平时默认本地验证优先；只有项目计划完成、阶段性完成、或明确要做发布级确认时，才跑远端 workflow
- 远端 workflow 触发后默认不要一直前台 watch；先记 run，再继续本地工作，默认也不要求专门回头核对最终结果，除非用户明确要求或后续任务依赖它
- 每次 `push` 之前先确认本地验证结果可靠，不要把 GitHub Actions 当日常排错主回路
- `build-discovery-search` 已经是有效命令，输入是关键词列表，不是 URL 列表
- `discovery_search` 的输出已经并入 `source-governance.json`，但仍属于候选建议，不是已验证配置
- `anthropic-news` 已完成收口：官方 RSS/feed 候选 404，当前正式入口保持为已远端验证的 `https://www.anthropic.com/news` listing
- `smoke-run-once-via-litellm.ps1` 检测到 MiniMax key 时，会自动切到完整 alias 配置；不要让本地 `LITELLM_CONFIG_PATH` 把它锁死在 DeepSeek-only local config
- `llm_client.py` 现在的指标语义是：
  - 同一 provider 下不同 alias model -> `provider` 保持不变
  - `model` 才记为 `mixed`
- `llm_client.py` 现在额外支持：
  - 主 provider 失败 -> 备用 provider 重试一次
  - latency/token 超 budget -> 备用 provider 重试一次
  - 仍失败 -> 交回 pipeline 走现有 fallback
- `ai_metrics` 现在会额外写：
  - `backup_stages`
  - `guardrail_stages`
  - 每个 stage 的 `attempts / backup_used / guardrail_reason / budget`

## 6.5 建库经验 / 防踩坑

- 先做 artifact，再做自动化；没有 artifact 的自动化几乎无法交接
- `candidate-updates.json` 是“接近执行的清单”，不是“无脑自动应用命令”
- 现在 `candidate-updates.json` 也会承载 `watch_update`；当前它应该尽量接近空队列，避免把已消化工作继续堆在里面
- `changedetection-watch-registry.json` 现在表示 repo 内 watch 状态；`planned/requested -> active_local -> blocked/unchanged/changed` 是当前推荐最小流转
- `watch-run-results.json` 是本地 watch 检查的最新结果，优先看 `blocked` 和 `changed`
- `apply_ready=false` 不是失败，它只是提醒我们当前还缺验证证据
- 这次 `anthropic-news` 的经验很典型：不要为了追求 RSS 形态去破坏一个已验证可用的 listing
- 远端 workflow 触发后不要一直前台 watch，记录 run 后继续本地推进；默认不需要再专门补看最终结果
- LiteLLM 本地接入时，先区分两类配置：
  - `litellm-config.local.example.yaml`：DeepSeek-only bootstrap
  - `litellm-config.example.yaml`：完整 stage alias 验证
- 如果发现 `summary/prefilter` 没走 alias，先查：
  1. `AI_API_KEY` / `LITELLM_MINIMAX_API_KEY` 是否可用
  2. `LITELLM_CONFIG_PATH` 是否误指向 local-only config
- Langfuse 首轮不要一上来全量上传 prompt / response；metadata-first 更稳，更适合先收工程可观测性
- `P3-B` 建库经验补充：
  - 飞书 API 在当前 profile 下可能不能：
    - 删除旧表
    - 删除旧 dashboard
    - 删除 dashboard block
    - 强制设置所有视图可见字段顺序
  - 更稳的做法是：
    - 能删就删
    - 删不掉就改名成“旧版…（停用）”
  - `status` 这类想做成单选字段时，直接用 `options` 顶层字段更稳
  - 仓库仍保持英文标准状态值，飞书可以显示中文状态，pull-back 时再映射回英文标准值
- 更安全的本地清理优先级是先删：
  - `.codex-pytest-temp*`
  - `.codex-tmp`
  - `.pytest_cache`
  - `.pytest-codex-*`
  - `.pytest-tmp`
  - `codex_pytest_temp`
  - `pytest-tempdir*`
- 不要把以下目录当成“临时垃圾”直接删：
  - `data/`
  - `docs/`
  - `out/`
  - `docs/superpowers/`
- 如果删除这类本地临时目录时遇到 `Access denied`，优先判断是不是测试进程残留或 ACL 异常；不要为了清理临时目录去误删正式交付目录

## 7. 下次接手的最短恢复流程

1. `git status --short`
2. `git log --oneline -5`
3. 读 `AI对接手册.md`
4. 读 `data/state/run-status.json`
5. 读 `out/source-governance/source-governance.json`
6. 如需验证 LiteLLM，先跑 `pwsh ./scripts/smoke-run-once-via-litellm.ps1`
7. 如需探源，再跑 `python -m auto_report.cli build-discovery-search --keywords config/source_discovery/keywords.txt`
8. 默认先看 `changedetection-watch-registry.json`、`watch-run-results.json` 和发布级本地验收结果，再讨论公开站整理 / 优化方案；不要回头重做 `P3-C-1 / P3-C-2 / P3-C-3`

## 8. 如果下一位只剩 3 分钟

先读：

1. 本文件
2. `AI对接手册.md`
3. `data/state/run-status.json`
4. `out/review-queue/candidate-updates.json`
5. `out/source-governance/changedetection-watch-registry.json`
6. `out/source-governance/watch-run-results.json`
7. `用户操作手册.md`

然后默认下一步：

1. 本地验证
2. 先看本地 watch runner 有没有 `blocked` / `changed`
3. 再进入公开站整理 / 优化方案讨论；方案确认前不要改 Pages
4. `push`
5. 手动触发一次 `Collect And Report`
6. 如后续任务依赖远端结果，再回头检查
