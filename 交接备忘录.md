# 交接备忘录

> 仓库：`D:\GitHub\auto`
>
> 用途：只保留当前状态、关键决策和最短恢复动作
>
> 最后更新：2026-05-08

## 1. 30 秒状态

- 当前直接在 `main` 上工作
- V1 已完成工程收口；后续路线统一进入 `V2升级方案.md`
- 最新完整远端发布确认：`Collect And Report` run `25528951290` success
- 远端分支头已推进到：`aa2fff78 chore: update pages site [skip ci]`
- 当前运行态基线：`data/state/run-status.json`
- 当前公开站入口：`https://wsysgz.github.io/VS_AI/`
- 当前测试基线：`374 passed`
- 当前远端最新自动日报快照：`generated_at=2026-05-08T08:19:05.206948+08:00`
- 当前远端最新自动日报状态：`publication_mode=auto`、`report_topics=194`、`risk_level=medium`
- 当前本地完成批次：
  - 新增 `amd-newsroom`、`ernie-blog`
  - 补齐启用源 comparison metadata
  - 夯实分析链证据归因、external enrichment 触发和国内外对比语义

## 2. 当前关键决策

- 飞书是唯一继续维护的交付通道
- 公开站 IA V2 已完成，不回头重做前四批
- `renesas-blog` 已确认采用官方 sitemap + `curl` 路线；不再做 requests-only 修复
- `ernie-blog` 已确认采用官方 RSS；`amd-newsroom` 已确认采用官方 newsroom listing；`nxp-edge-ai` 继续停留在 repo fallback，不把当前不稳定官方 blog 提升为正式 source
- 本地验证由 Codex 主导；DeepSeek 只作为输出对照
- `out/`、`output/`、`.pytest_*`、`.codex_*` 视为可重建副产物
- 分析链当前语义固定为：
  - `support_evidence` 只保留 `direct_support`
  - `related_context` 只作背景，不再直接降风险
  - `track_snapshots` 表示同赛道代表信号，`head_to_head` 只表示可直接配对的中外事件
- 顶层手册已收口为：
  - `README.md`
  - `工程手册.md`
  - `V2升级方案.md`
  - `交接备忘录.md`

## 3. 当前没有阻塞的事

- `Collect And Report` 主链远端成功
- Pages、ops dashboard、review queue 已随远端链成功生成
- 公开站 IA V2 已完成
- 来源治理尾项没有新的发布阻塞

## 4. 当前仍需长期运营观察的点

- `renesas-blog`：维持官方 sitemap 证据链与 watch 观察
- `nxp-edge-ai`：继续观察是否出现稳定官方 feed / API / 可重复抓取 listing；在此之前维持 repo fallback
- repo-local watch：继续消费变化项，但它属于持续运营，不是当前工程未完成项
- 自动产物提交会在远端 run 后推进 `data/`；做新开发前先 `git pull --ff-only`

## 5. 下次接手最短流程

```powershell
git pull --ff-only origin main
git status --short
git log --oneline -5
gh run list --workflow collect-report.yml --branch main --limit 3
```

然后按顺序看：

1. 本文件
2. `data/state/run-status.json`
3. `工程手册.md`
4. `V2升级方案.md`

## 6. 如果只剩 3 分钟

先记住：

- 当前仓库已经是发布态
- 当前主要工作是 V2 规划或持续运营，不是回头补 V1
- 先确认本地是否落后远端自动产物提交
- 只要不是明确发布确认，不要把 GitHub Actions 当调试主回路
