# 交接备忘录

> 仓库：`D:\GitHub\auto`
>
> 更新时间：2026-04-16

这份备忘录只写“下一次继续干活最容易丢的上下文”，用于防止会话中断、切人接手或对话历史丢失。

## 1. 当前状态

- 当前主线已经直接落在 `main`
- 当前方向是 **P1：来源稳定性升级**
- 当前真实验证基线是：`200 passed`
- 当前仓库统一文档入口已经换成根目录：
  - `README.md`
  - `AI对接手册.md`
  - `用户操作手册.md`
  - `V1升级方案.md`

## 2. 最近已完成事项

### PushPlus / ClawBot

- 已完成 `clawbot` 最终送达验证，不再只看 `/send` 的 `code=200`
- 已接入：
  - `PUSHPLUS_SECRETKEY`
  - 开放接口
  - 安全 IP 白名单
- 当前成功判定以这两个值为准：
  - `haveContextToken=1`
  - `delivery_status=2`

### 文档

- 旧手册已删除：
  - `docs/HANDOFF.md`
  - `docs/OPS_RUNBOOK.md`
  - `docs/USER_GUIDE.md`
- 新总手册和用户手册已建立：
  - `AI对接手册.md`
  - `用户操作手册.md`

### P1 第一阶段

- `NVIDIA/cuda-cmake` 已替换为 `NVIDIA/TensorRT`
- `st-blog` 已禁用
- `ti-e2e-blog` 已禁用
- `arxiv-cs-ai` 已切到官方 Atom 查询接口
- `source_health` 已支持 per-source breakdown
- ops dashboard 已新增 `Source Failure Breakdown`

## 3. 用户明确偏好

- **不要建分支，直接在 `main` 上工作**
- **Telegram 当前视为网络环境问题，不用花时间修**
- 优先继续做 **P1：来源稳定性升级**
- `README.md` 和总手册的大改版、重写、重排版，等 V1 基本完成后再做

## 4. 当前不要忘的事实

- `run-status.json` 仍然是唯一权威运行态文件
- `workflow_dispatch` 只跑远端已 push 代码
- PushPlus 现在已经不是“接口成功就算成功”，而是“最终送达状态成功才算成功”
- 现在的重点不在 Telegram，不在换架构，而在 **把脆弱来源治理做实**

## 5. 下一步建议顺序

1. 为高价值脆弱来源寻找 RSSHub route
2. 梳理 changedetection.io watch 清单
3. 给 source registry 增加分层与替代入口字段
4. 让 `source_health` 输出更适合直接运维决策

## 6. 建议先看的文件

- `AI对接手册.md`
- `V1升级方案.md`
- `src/auto_report/app.py`
- `src/auto_report/outputs/ops_dashboard.py`
- `config/sources/github.yaml`
- `config/sources/websites.yaml`
- `config/sources/rss.yaml`

## 7. 这份备忘录的用途

如果下一次会话只剩 3 分钟上下文，就先读这份文件，再继续干活。
